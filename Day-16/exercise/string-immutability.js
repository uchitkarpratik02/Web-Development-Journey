let city = "Nagpur";

city.toUpperCase(); //because string not inthis line only string update version print but it cannot changed

console.log(city);

city = city.toUpperCase(); //in this scenarion updated version stored in the city 
console.log(city);